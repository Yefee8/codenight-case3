# Server-side gateway URL. Docker Compose overrides this with http://gateway.
GATEWAY_URL=http://localhost:8080

# Generate with: openssl rand -base64 32
AUTH_SECRET=replace-with-a-random-secret

# false for local HTTP; true only when the browser uses HTTPS.
COOKIE_SECURE=false
