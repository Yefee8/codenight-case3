[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    docker: requires a local Docker Engine
    integration: crosses a process or infrastructure boundary
    security: verifies a security invariant

